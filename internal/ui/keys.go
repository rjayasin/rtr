package ui

// Per-screen help footers. Kept as plain strings (rather than a bubbles/key
// binding set) since rtr's keymap is small and fixed.
const (
	helpBookmarks = "↑/↓ move • enter connect • n new • e edit • d delete"
	helpForm      = "tab/↑↓ field • enter save • esc cancel"
)
